package ${package.Service};

import ${package.Entity}.${Entity};
import com.baomidou.mybatisplus.extension.service.IService;

/**
 * <p>
 * ${table.comment!} 服务类
 * </p>
 *
 * @author ${author}
 * @since <%= LocalDate.now().toString() %>
 */
public interface ${Entity}Service extends IService<${Entity}> {
}
